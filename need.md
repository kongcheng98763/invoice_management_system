# 发票管理系统 — 需求与实施规格（need.md）

> 版本：v0.4 ／ 日期：2026-09-19
> 技术栈：Python 后端 + SQLite 数据库 + WinUI3 桌面界面
> 定位：单人使用的本地报销发票汇总与建档工具

系统目标：把散落的报销发票（PDF）统一提取、归档、建档，维护一张与现有 `报销表.xlsm` 等价的报销总述，并对归档文件夹的实际内容与系统内记录做持续对账。

> **v0.4 修订（回退）**：归档目录确认为**三层**——年 / 月 / **单次报销文件夹**（G 列实测样例的路径顺序即为此结构；v0.2 改成的"月目录平铺"系依据一次口误答复，现撤回）。**归属由此变为目录直读**（一个报销文件夹 = 一次报销），4.5 从"核心难点"降级为"扫描规则 + 三种边界情形"，**Q4 消解**，按票面要素聚类自动分组降为首版外增强。
>
> **v0.3 修订**：确定分类种子数据（5.3）与要件规则表建为空表（Q5）；比对清单与实测数据后发现**大类/项目归属在历史数据中存在错配**，新增 5.2-⑤ 及其对导入清洗的影响，Q6 待结。
>
> **v0.2 修订**：明确一次报销由**多个不同类型的文件**构成（材料费 = 发票 + 入库单 + 支付记录 …），且份数不定。据此把数据模型从「报销 → 发票 → 文件」改为「报销 → 文件（带要件类型），票面信息为票据类文件的扩展」；新增归属机制讨论（4.5）与汇总一致性校验（5.1）。

---

## 一、注意事项

### 1.1 Agent / 脚本中间产物的放置位置

**规则：所有在工作过程中产生的中间文件，一律落到 `invoice_management_system\temp\`。** 不得散落在工作区根目录，也不得写进 `invoice_recognition\` 或 `winui\` 这两个子项目内部。

| 产物类型 | 位置 |
|---|---|
| 一次性分析产物、草稿、探查 dump | `temp\` |
| 调试日志、解析结果缓存 | `temp\` |
| 对账 diff 报告、临时导出的 Excel 样本 | `temp\` |
| 各票种脱敏测试 PDF | `invoice_recognition\tests\samples\`（唯一例外，属于测试资产，需长期保留） |

`temp\` 的定位是**非交付物、可随时整体清空**。任何模块都不得把 `temp\` 中的文件当作持久依赖或数据源读取。

本轮已发生的违规产物 `excel_dump.txt`、`excel_full_dump.txt`（读取 `报销表.xlsm` 时产生的分析文件）已移入 `temp\`。

### 1.2 交付物位置

`need.md` 及后续输出的文档、报告，直接放在工作区根目录 `invoice_management_system\`。

### 1.3 归档目录规范

```
报销/
├── 2026年/
│   ├── 04月/
│   │   └── 2026-04-24 模具合同 …/        ← 一次报销一个文件夹
│   ├── 05月/
│   └── 06月/
│       └── 2026-06-09 差旅费 …/          ← 该次报销的全部文件放在这里
└── 2027年/
    └── ...
```

- **三层**：年（`{YYYY}年`）→ 月（`{MM}月`）→ **单次报销文件夹**。三层均为必需。
- 单次报销文件夹命名形如 `{YYYY-MM-DD} {报销项目} {自由说明}`，日期取该次报销的经手时间（B 列）。
- **一次报销的全部文件归入同一个报销文件夹**，该文件夹整体作为一个归档单元处理 —— 这是本系统的归属依据（见 4.5）。
- 依据是 G 列唯一的实测样例：`2026年\06月\2026-06-09 差旅费 是撒旦发生`，其**路径顺序即为上述三层结构**（末尾 `是撒旦发生` 为误输入文字，不属结构）。
- 一次报销的文件份数**不固定**：可能多张发票一起报销，此时入库单、支付记录等其他要件也可能相应有多份（见 5.2-④）。

> **本条为 v0.4 回退**：v0.2 曾依据一次答复把结构改为"月目录平铺、不建报销子文件夹"，该答复系使用者口误，现恢复为三层。**随之 4.5 的归属问题大幅简化**（目录本身即归属依据），Q4 已基本消解。
>
> 另需说明：即便如此，G 列作为**人工填写的路径**依然不可靠 —— 25 行里只有 1 行填了，且那 1 行还混入了误输入字符。因此系统不读 G 列来推断归属，而是**以扫描目录得到的结果为准，反向生成/校验汇总表里的文件位置**。

---

## 二、前端（WinUI3 / C#）

### 2.1 已确认范围

单人本地使用，**不做登录、不做权限管理**，因此界面上没有任何账号体系相关的设计。

---

## 三、通讯方式

### 3.1 桥接方案

**采用方案 A：Python 侧常驻本地 HTTP 服务（FastAPI），WinUI3 通过 REST + WebSocket 与之通信。**

---

## 四、后端（Python）

### 4.1 现状承接

`invoice_recognition\` 目前只有配置，没有业务代码：

- `config.py` — `base_path = r".\报销"`、`excel_filename = r"报销表.xlsm"`、`category_map`（7 条：差旅费/会员费/版面费/餐饮费/材料费 → 日常报销；学生劳务费/专家咨询费 → 酬金申报）
- `pyproject.toml` — 名称 `invoice-recognition`、Python `>=3.12`，**`dependencies = []` 为空**
- `test.py` — 空文件
- `报销表.xlsm` — 现有模板，含 1 个 Sheet「报销总述」、25 行实际数据（含表头共 26 行）

> `category_map` 与实际数据不一致，见 5.2-①。


## 五、数据库的结构（SQLite）

设计基准是现有 `报销表.xlsm` 的 Sheet「报销总述」，共 13 列、25 行真实数据。逐列的实测结论与建表处理：

| 列 | 字段 | 实测数据形态 | 建表处理 |
|---|---|---|---|
| A | 序列 | `1..25` 连续整数 | `id INTEGER PRIMARY KEY AUTOINCREMENT` |
| B | 经手时间 | `2026.04.24`（点分隔） | `happened_on TEXT` 存 ISO `YYYY-MM-DD`，导入时归一 |
| C | 报销大类 | 日常报销 / 酬金申报 / 国内差旅 / `/` | `category_id INTEGER NULL` → `categories` |
| D | 报销项目 | 餐饮费、差旅费、学生劳务费、专家咨询费、会员费、版面费、材料费、审稿费、模具合同、项目调整说明 | `item_id INTEGER NULL` → `expense_items` |
| E | 报销人（得钱方） | 25 行中全部为空 | `payee TEXT NULL`，字段保留 |
| F | 金额 | `49800`／`419.1`／`1232.5`／`574(只报这部分)+50(押金不报)`／`/` | **拆三列**，见 5.2-② |
| G | 文件位置 | 绝大多数为空；仅一处 `2026年\06月\2026-06-09 差旅费 …` | `files` 表外键，路径不再手填 |
| H | 报销单号 | 13 行有值，均为 6 位数字（`476785`、`473994`…）；另见 `/` | `form_no TEXT NULL`（标识符而非数量，且混有 `/`，故不用整数） |
| I | 项目负责人 | 全空 | `owner TEXT NULL`，字段保留 |
| J | 备注1 | `退回需补充发票`、`A4纸要走招投标流程，还需要备案表、确认书、采购申请`、`常州`、`金华`、`共3147` | `note1 TEXT NULL` |
| K | 备注2 | `额外800(餐补)+640(交通补贴)`、`原件未改重新上传2026.5.8`、`去掉纸质品,重新报` | `note2 TEXT NULL` |
| L | 完成 | `√`、`√ `（带尾随空格）、`×`、`流程中` | `status TEXT NOT NULL` 枚举，见 5.2-③ |
| M | 还需完成项目 | 全空 | 独立 `todos` 表（一对多），见 5.1 |

> **G 列的现状值得单独注意**：25 行里只有 1 行填了文件位置，且填的那行内容末尾还混入了 `是撒旦发生` 这样的误输入。这说明"文件位置"这一列在人工维护下几乎失效——这正是系统要用 `files` 表 + 自动对账取代手填路径的直接理由。
